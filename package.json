{
  "name": "delhi-ka-khel",
  "version": "1.0.0",
  "description": "On-Chain Tic Tac Toe on Monad Testnet - Delhi Ka Khel!",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network monadTestnet",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "frontend": "cd frontend && npm run dev",
    "build": "hardhat compile && cd frontend && npm run build"
  },
  "keywords": ["monad", "blockchain", "tictactoe", "delhi", "hackathon"],
  "author": "Monad Blitz Participant",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "hardhat": "^3.2.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
